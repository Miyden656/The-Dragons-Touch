"""Strategy bridge — report section modules used by the live report pipeline.

Active report builders, the report viewer page, and the section registry call
these section/prompt/viewer/payload builders. The package was previously named
`reports.legacy`, which was misleading: these modules back live strategy, mana
base, and commander-build sections — not optional or deprecated content.
"""
