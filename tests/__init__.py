"""Smoke tests for The Dragon's Touch v1.5.

Each test_*.py file is self-contained, runs without pytest, prints PASS/FAIL,
and exits 0 on success / 1 on failure. Run them all via `py -3 tests/run_all.py`
or one at a time via `py -3 tests/<file>.py` from the project root.
"""
