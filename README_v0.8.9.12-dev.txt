# TDT v0.8.9.12-dev — Stable v0.8 Full Regression Pass

Apply from project root:

```powershell
py tools/apply_v0.8.9.12_stable_v08_full_regression_pass.py
```

This patch updates known version labels, writes the full Stable v0.8 regression checklist, and runs compile checks. It does not change backend behavior, UI flow, combo matcher logic, parser logic, index builder logic, or API behavior.
