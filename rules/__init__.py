"""The Dragon's Touch — rules reference package.

v1.6.1 Phase 5 promotes the bracket reference from prose-only (.md) to a
maintainable Python data module (`bracket_definitions.py`) that the runtime
filter actually reads. The .md files in this directory remain the
human-readable companion docs — when the rules change, update both.

Future phases (Phase 6) will add `commander_format_rules.py` for the deck
construction rules (100 cards, singleton + basic exception, color identity,
partner / background / Friends Forever / Doctor's Companion, etc.).
"""
